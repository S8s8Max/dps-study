---
all:
  hosts:
    pi-master:
      ansible_host: 192.168.1.100   # ← 実際の IP アドレスに変更する

  children:
    masters:
      hosts:
        pi-master:

    workers:
      hosts: {}   # 将来 pi-worker-01 などを追加する
